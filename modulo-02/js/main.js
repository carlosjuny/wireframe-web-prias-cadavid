/* ============================================================
   MÓDULO 02 · ARRANQUE
   1) casos-data.js + casos-penales.js: siembra datos del expediente
   2) modulo02.js: init() existente (rol, agentes, casos, FAQ, config, ayuda)
   ============================================================ */
initCasosPenales();
init();
